

const Alert = ({message, title}) => {
  return (
    <article>

        <article>
            <h2>{title}</h2>
            <p>{message}</p>
            <button>
                
            </button>
        </article>

    </article>
  )
}

export default Alert